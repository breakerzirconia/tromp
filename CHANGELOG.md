# v0.1.1

### Added

* A `h(ybrid)` rendering mode that constructs the diagram with block elements.

### Changed

* Moved the `BoxDrawings` module definitions to the `Rendering.Text` module.

# v0.1.0

### Added

* The first version of the tool with the only available rendering mode being `t(ext)`.
